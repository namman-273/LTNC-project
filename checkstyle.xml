<?xml version="1.0"?>
<!DOCTYPE module PUBLIC "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
    "https://checkstyle.org/dtds/configuration_1_3.dtd">

<!--
    Checkstyle — Dự án Đấu Giá Trực Tuyến
    Áp dụng coding convention thực tế, tương thích với code IntelliJ IDEA.
    severity="error" => vi phạm FAIL BUILD.
-->
<module name="Checker">
    <property name="severity" value="error"/>
    <property name="fileExtensions" value="java"/>

    <!-- Không dùng tab — chỉ dùng spaces -->
    <module name="FileTabCharacter">
        <property name="eachLine" value="true"/>
    </module>

    <!-- Độ dài dòng tối đa 150 ký tự — tránh wrap dòng không cần thiết -->
    <module name="LineLength">
        <property name="max" value="150"/>
        <property name="ignorePattern"
            value="^package\s|^import\s|^\s*\*\s*(https?://|@)"/>
    </module>

    <!-- Không để trailing whitespace cuối dòng -->
    <module name="RegexpSingleline">
        <property name="format" value="\s+$"/>
        <property name="minimum" value="0"/>
        <property name="maximum" value="0"/>
        <property name="message" value="Dong co khoang trang thua o cuoi (trailing whitespace)."/>
    </module>

    <!-- Hỗ trợ @SuppressWarnings("checkstyle:...") trong code -->
    <module name="SuppressWarningsFilter"/>

    <module name="TreeWalker">

        <!-- ===== NAMING CONVENTIONS ===== -->

        <!-- Package: chữ thường, không underscore -->
        <module name="PackageName">
            <property name="format" value="^[a-z]+(\.[a-z][a-z0-9]*)*$"/>
        </module>

        <!-- Class/Interface/Enum: UpperCamelCase -->
        <module name="TypeName"/>

        <!-- Method: lowerCamelCase -->
        <module name="MethodName"/>

        <!-- Constant (static final): UPPER_SNAKE_CASE -->
        <module name="ConstantName"/>

        <!-- Field, tham số, biến cục bộ: lowerCamelCase -->
        <module name="MemberName"/>
        <module name="ParameterName"/>
        <module name="LocalVariableName"/>
        <module name="StaticVariableName"/>

        <!-- ===== IMPORTS ===== -->

        <!-- Import trùng lặp -->
        <module name="RedundantImport"/>

        <!-- Import không được dùng -->
        <module name="UnusedImports">
            <property name="processJavadoc" value="false"/>
        </module>

        <!-- ===== BRACES ===== -->

        <!-- Luôn dùng {} cho if/else/for/while/do -->
        <module name="NeedBraces"/>

        <!-- Dấu { ở cuối dòng (K&R style) -->
        <module name="LeftCurly"/>

        <!-- Dấu } đứng riêng một dòng, ngoại trừ else/catch/finally -->
        <module name="RightCurly"/>

        <!-- ===== INDENTATION ===== -->

        <!-- 4 spaces — chuẩn IntelliJ IDEA -->
        <module name="Indentation">
            <property name="basicOffset" value="4"/>
            <property name="braceAdjustment" value="0"/>
            <property name="caseIndent" value="4"/>
            <property name="throwsIndent" value="8"/>
            <property name="lineWrappingIndentation" value="8"/>
            <property name="arrayInitIndent" value="4"/>
        </module>

        <!-- ===== CODING PRACTICES ===== -->

        <!-- Không để catch rỗng hoàn toàn (không có comment) -->
        <module name="EmptyCatchBlock">
            <property name="exceptionVariableName" value="expected|ignored"/>
        </module>

        <!-- Không khai báo nhiều biến trên một dòng: int a, b; -->
        <module name="MultipleVariableDeclarations"/>

        <!-- Kiểu mảng phải viết sau tên kiểu: String[] args -->
        <module name="ArrayTypeStyle"/>

        <!-- switch phải có default case -->
        <module name="MissingSwitchDefault"/>

        <!-- Long literal phải dùng L hoa: 100L, không phải 100l -->
        <module name="UpperEll"/>

        <!-- Thứ tự modifier: public static final... -->
        <module name="ModifierOrder"/>

        <!-- Hỗ trợ @SuppressWarnings annotation bên trong TreeWalker -->
        <module name="SuppressWarningsHolder"/>

    </module>
</module>
